# Images

`hero.svg` is a placeholder illustration so the site ships without licensed
photography. Replace it with a real photo (`hero.jpg`, ~1600px wide, under
200 KB) that matches the design brief: a sharp chef's knife on a board with
fresh produce. Update the `<img>` in `src/sections/Hero.tsx` and the preload
link in `index.html`.
